apiVersion: v1
kind: Service
metadata:
  name: password-generator-service
spec:
  type: NodePort
  selector:
    app: password-generator
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
